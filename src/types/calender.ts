export interface CalenderForm {
    name: string;
    description: string;
}

export interface CalenderRequest {
    name: string;
    description: string;
}
export interface CalenderResponse {
    name: string;
    description: string;
}
